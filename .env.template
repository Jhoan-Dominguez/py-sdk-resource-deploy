AWS_REGION=MY_AWS_REGION
AWS_ACCOUNT_ID=MY_AWS_ACCOUNT_ID
PROJECT_NAME=MY_PROJECT_NAME
TRUSTED_PRINCIPAL_USER=MY_IAM_USER_NAME
INVENTORY_TABLE=MY_INVENTORY_TABLE_NAME
# Optional: create (default, the tool creates the table if missing) | existing (never create it)
INVENTORY_TABLE_MODE=create
